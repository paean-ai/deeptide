# Canvas Tower Defense

A focused tower defense template with a fixed path, build pads, wave spawning, tower placement/upgrades, targeting, projectiles, rewards, and base lives.

## Run

Open `index.html` directly, or serve the folder:

```bash
python3 -m http.server 4186
```
