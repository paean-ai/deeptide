[package]
name = "deeptide-host"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Shared agent-assembly layer for Deeptide — backend/config/session wiring used by both the CLI and the GUI."

[dependencies]
deeptide-core = { path = "../deeptide-core" }

[lints]
workspace = true
